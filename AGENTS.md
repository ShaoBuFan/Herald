# AGENTS.md

> 本文件是本仓库的宪章：既约束 AI 助手的行为，也记录项目的目标、路线与约定。
>
> **它是活文档。** 每完成一个阶段都要回来更新「路线图与当前进度」一节。

---

## 1. 这个项目是什么

Herald 是一个**从零构建的 AI 编程 agent harness**（智能体框架），对标市面上的 ZCode、DeepSeek Harness（dsh）、Codex CLI、Claude Code。

但它的第一属性是 **学习成长型项目**，不是产品。

- **目标**：真正想明白一个编程 agent 是怎么运转的——每一层为什么这么设计、否决过哪些方案、代价是什么。
- **成功标准**：作者能用自己的话讲清每个设计决策的理由。**代码能跑只是必要条件，不是充分条件。**
- **市面上的实现是教材，不是模板。** 研究它们的设计取舍，远比照着抄一遍有价值。

---

## 2. 给 AI 助手的硬性规则

> 这一节是本文件最重要的部分。**任何 AI 助手在本仓库动手前必须先读这一节。**

### 2.1 核心逻辑由人类作者编写，AI 不得代劳

作者本人编写所有**需要做设计决策**的代码：

- 事件日志与会话模型
- LLM 适配器（流式解析、工具调用重组）
- Agent 循环
- 工具注册表与执行语义
- 权限判定
- 上下文压缩策略

**AI 不得在未被明确要求的情况下实现上述任何内容。** 哪怕作者只是说"这里有点麻烦"、"这段怎么写"，那也不是授权——那是讨论邀请，不是实现委托。

### 2.2 卡住时的默认行为：给提示，不给答案

作者求助时，默认按以下顺序响应：

1. **反问**他自己的判断（"你觉得这里为什么会出问题？"）
2. **指方向**：相关概念、需要查阅的位置、值得读的源码
3. **给最小可运行示例**来说明某个语言机制——注意是机制示例，不是他正卡住的那段代码

只有当作者**明确说**"我卡死了，给我看答案"时，才提供完整实现。即便如此，给出之后也要讲透原理，确保不是"贴上去能跑就行"。

### 2.3 遇到语言机制要就地讲解

作者的 TypeScript 水平是"会写业务代码"。项目中必然遇到下面这些机制，**遇到时就地展开讲**，并配一个能单独跑的小例子，而不是默默写完：

| 机制 | 会在哪里遇到 |
|---|---|
| `AsyncGenerator` / `AsyncIterable` / `for await...of` | 流式响应 |
| `AbortSignal` / `AbortController` | 取消传播 |
| 判别联合 + 穷尽性检查 | 事件类型定义 |
| `Promise.allSettled` vs `Promise.all` | 工具并发 |
| `satisfies` 与泛型约束 | 工具参数类型安全 |
| ESM `type: module` 与 `.js` 后缀导入 | 模块系统（高频坑） |
| `strict` + `noUncheckedIndexedAccess` 下的写法 | 全程 |

### 2.4 AI 可以代劳的范围

以下属于"体力活或配置噪音"，AI 可以直接做：

- 脚手架与环境配置（`package.json`、`tsconfig`、vitest 配置、workspace）
- 测试替身与夹具（`ScriptedAdapter`、临时目录工具、fake FS）
- 环境排错（依赖装不上、构建报错、版本冲突）
- 代码 review——指出设计问题，而不是直接改
- 资料检索与整理（读 dsh 源码后给出摘要、对比表）

> **边界原则**：写它不产生理解的工作，AI 可以做；写它本身就是思考过程的工作，归作者。

### 2.5 review 的姿态

AI 做 review 时**指出问题和权衡，而不是顺手改掉**。发现问题时说清"这里可能的后果是什么"，让作者自己决定怎么改。

---

## 3. 学习和探讨的方式

每个能力模块走固定的**三段式**：

1. **自己设计并实现** —— 先写下自己的方案，哪怕粗糙
2. **对照市面实现** —— 读 dsh / ZCode / Codex / Claude Code 的对应部分，尤其是**它们否决过的方案**
3. **回来改进** —— 带着"我是这么解的，它为什么那么解"的判断去改

关键设计决策写进 [`docs/decisions/`](docs/decisions/)（ADR 格式），一段话即可：**问题是什么、选了哪个方案、否决了什么、代价是什么**。

> 被否决的选项往往比最终方案更有教学价值——它回答的是"为什么不能那么做"。

### 复述验收

每个阶段结束时，作者用**自己的话**讲一遍：这个设计解决了什么问题、否决了哪些方案、代价是什么。**讲不顺的地方就是没懂的地方**，回头补。

---

## 4. 路线图与当前进度

### 阶段一：最小闭环（建立体感）

**交付物**：`herald run "读一下 README 并总结"` 能完整跑通。

链路：用户输入 → 构造请求 → LLM 流式返回 → 解析出工具调用 → 执行读文件 → 结果回灌 → 模型继续 → 输出终稿。

作者要写的：`LlmAdapter` 接口 + OpenAI 兼容适配器、最简单的消息数组（**刻意先不用事件日志**）、agent 循环、一个 `read_file` 工具、CLI 入口。

> **关于"刻意先不用事件日志"**：这是设计好的教学路径。先体会普通消息数组在哪里疼——无法恢复、无法审计、配对容易错、想同时喂给两个模型就得复制。**先疼，下一阶段引入事件日志时才知道它到底换来了什么。**

- [ ] `LlmAdapter` 接口设计
- [ ] OpenAI 兼容适配器（SSE 解析 + 工具调用分片重组）
- [ ] 消息数组
- [ ] Agent 循环
- [ ] `read_file` 工具
- [ ] CLI 入口
- [ ] 阶段复述验收

### 阶段二：地基深挖（三个硬协议）

每个协议走：**先问作者 → 他答 → 补充市面做法 → 他改设计**。

- [ ] **事件日志该记什么**——追加式日志 + 派生模型消息，为什么强于直接维护数组？拿阶段一的代码当靶子，逼出它在四种场景下的失效：进程被杀、想回滚、想审计、想同时喂两个模型
- [ ] **LLM 流式协议**——SSE 帧边界、`data: [DONE]` 的意义、tool_call 分片为什么必须按 index 累加而非覆盖、`usage` 何时到达、流中途断了该重试还是报错
- [ ] **工具调用的并发与失败语义**——只读并发/写串行防的是什么竞态？同批中一个失败其他怎么办？模型请求不存在的工具怎么办？参数不合法呢？

### 阶段三：以"对照 dsh"为方法，逐个加厚能力

每个能力走三段式：**自己设计实现 → 读 dsh 对应模块 → 改进**。

- [ ] 事件日志替换数组 + JSONL 持久化 + 恢复
- [ ] 工具集扩充（`write` / `edit` / `glob` / `grep` / `bash`）+ 参数校验
- [ ] 权限系统（模式、路径围栏、命令策略）
- [ ] 上下文管理（token 估算、结果裁剪、压缩）
- [ ] 多 provider（Anthropic）+ 模型切换
- [ ] 系统提示装配 + `AGENTS.md` 加载

### 阶段四：Web UI（后补）

CLI 跑顺之后再做，当作独立的前端学习课题（React + Vite + SSE + REST），不跟内核设计混在一起。

---

## 5. 仓库结构

当前仓库内容：`README.md`、`AGENTS.md`、`docs/`（主要工作区，见 [`docs/README.md`](docs/README.md)）、`LICENSE`、`.gitignore`、`.gitattributes`。

**代码目录结构尚未确定。** 等作者自己定下技术选型和模块划分之后再写这一节，不要提前固化。可预见的一段时间内工作重心在 `docs/`，不在代码。

先记一条原则：**如果最终拆出内核层，内核的运行时依赖应尽量保持为零。** 这不是洁癖——它会逼你自己写 SSE 解析、自己处理流式工具调用重组，而这些恰恰是最值得亲手写一遍的东西。

---

## 6. 命令

**尚未搭建，当前没有可用命令。** 等工具链定下来之后回填这一节。

**测试的硬性要求**（与用什么工具链无关）：内核测试必须**离线可跑**，不依赖 API key、不依赖网络。真实 API 的端到端测试在缺少 key 时自动跳过。

**环境事实**（本机已实测，供以后选型时参考）：

- Node 24.19 / pnpm 11.22 已装；npm 源可达，但冷装偏慢（约 1s 基线延迟，批量传输慢）。
- pnpm 11 起不再读取 `package.json` 的 `pnpm` 字段，设置统一放在 `pnpm-workspace.yaml`。另外它有"执行脚本前先检查依赖状态"的行为：若有依赖的安装脚本未获授权，会直接报 `ERR_PNPM_IGNORED_BUILDS` 并拒绝执行**任何**脚本，包括 `tsc`。将来排查"连类型检查都跑不起来"时先看这里。
- TypeScript 7 **已移除 `baseUrl`**，`paths` 直接相对于 `tsconfig.json` 解析。
- 本机 node-gyp 未配置（MSVC 在 D 盘且不在 PATH）。**需要本地编译的原生模块装不上**，选型时避开这一类依赖。

---

## 7. 技术约定

- **语言**：TypeScript，ESM（`"type": "module"`）。导入本地模块必须带 `.js` 后缀。
- **严格模式**：`strict` + `noUncheckedIndexedAccess`，全量开启。
- **运行时**：Node >= 22.19。
- **包管理**：pnpm。
- **原生模块**：**禁用**。本机 node-gyp 未配置（MSVC 在 D 盘且不在 PATH），任何需要本地编译的依赖（`better-sqlite3`、`node-pty`、`tiktoken` 等）一律不用。持久化用 JSONL 而非 SQLite，正是出于此。
- **注释与文档语言**：中文。
- **提交信息**：Conventional Commits（`feat:` / `fix:` / `docs:` / `chore:` / `refactor:` / `test:`），描述可用中文。

---

## 8. 非目标

以下**明确不在**当前范围内，避免范围蔓延。需要时再单独立项：

- MCP 客户端
- 子代理（subagent）
- Skills（`SKILL.md` 机制）
- 插件系统与 marketplace
- 工作流引擎
- 多用户 / 远程服务 / 云沙箱
- 终端 TUI（富交互）

> **但接缝要留好**：工具注册表就是 MCP 的挂载点，系统提示装配就是 Skills 的挂载点，`LlmAdapter` 就是新 provider 的挂载点。

---

## 9. 参考材料（本机已有）

| 材料 | 位置 | 用途 |
|---|---|---|
| DeepSeek Harness 源码 | `D:\CodeProject\AI\deepseek-harness\` | 工业级参照实现（84 万行 TS，267 包） |
| dsh 设计笔记 | 上述仓库的 `.agents/notes/` | **1910 篇设计笔记**，记录每个子系统的理由与被否决方案 |
| AGENT（早期实现） | `D:\CodeProject\AI\AGENT\` | 同类问题的一份对照答案 |

> **关于 AGENT**：它是很好的镜子，也是危险的拐杖。**建议在阶段一自己写完后再看**——那时它是有价值的对照；提前看就是抄，等于跳过整个学习过程。

dsh 子系统速查：

| 主题 | dsh 位置 |
|---|---|
| 事件日志与会话 | `packages/core/session`、`packages/session/session-persistence-jsonl` |
| LLM 流式 | `packages/llm/llm/src/assistant-stream.ts`、`packages/llm/llm-deepseek/src/` |
| 工具执行 | `packages/core/tools`、`docs/tool-execution-pipeline.md` |
| 权限与审批 | `packages/interaction/permission-presets`、`docs/subsystems/approval.md` |
| 上下文压缩 | `packages/compaction/compaction-basic`、`docs/subsystems/compaction.md` |
| 指令文件加载 | `packages/context/agent-instructions` |

---

## 10. 作者备忘

- **最常见的死法是停在阶段一。** 跑通一个能对话、能读文件的 demo 会带来强烈成就感，然后就没有然后了。对抗办法：每阶段有明确交付物与复述验收。
- **AI 不替你写核心逻辑，这条会坚持。** 你会遇到想让它直接给出实现的时刻——那是正常的，说明你正踩在值得学的地方。
